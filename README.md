# Canny-Edge-Detector
A system that detects all the edges in any given image, system built from scratch without the help of any library function, it consists of four modules: Gaussian smoothing, gradient operation, non-maxima suppression and thresholding.


